config:
  slug: superflow-extension
  name: superflow-extension
  ui:
    surfaces:
      - extension-team-configuration
      - extension-top-level-site-configuration
  build_event_handlers:
    - onPreBuild
    - onPostBuild
